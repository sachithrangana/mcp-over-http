# Public URLs, as reached from outside the containers. Defaults suit a local
# `docker compose up`; set them to your real hostnames before deploying.
PUBLIC_API_URL=http://localhost:8080
PUBLIC_MCP_URL=http://localhost:3000

# Host ports to publish on.
API_PORT=8080
MCP_PORT=3000
